[project]
name = "gameskill-fql"
version = "0.1.0"
description = "DINOv2 game vision-action policy trained with Flow Q-Learning"
requires-python = ">=3.11"
dependencies = [
  "datasets>=5.0",
  "onnx>=1.18",
  "onnxruntime>=1.20",
  "pillow>=11.0",
  "pyyaml>=6.0",
  "tensorboard>=2.21.0",
  "timm>=1.0",
  "torch>=2.3",
]

[tool.uv]
package = false
